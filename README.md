Copy this to download all pakcages needed:

**pip install numpy torchvision wonderwords sentence_transformers streamlit**

To run the project, put this into the terminal

**python -m streamlit run app.py**
